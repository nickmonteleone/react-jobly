/** renders login page
 *
 * props: authenticate()
 *
 * state: none
 *
 * RouteList -> Login -> loginForm
 */


function Login({ authenticate }) {

  return (
    <div>

    </div>
  )
}

export default Login;